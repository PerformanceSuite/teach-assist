import { GlobalLayout } from '../../components/GlobalLayout';

export default function TeachAssistLayout({ children }: { children: React.ReactNode }) {
  return <GlobalLayout>{children}</GlobalLayout>;
}
